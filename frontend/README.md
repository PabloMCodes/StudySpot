# Frontend Setup

This frontend uses Next.js App Router with Tailwind CSS.

## Run locally

1. Install dependencies:

   ```bash
   npm install
   ```

2. Start dev server:

   ```bash
   npm run dev
   ```

## Environment

Set backend API URL as needed:

```bash
NEXT_PUBLIC_API_URL=http://localhost:8000
```
